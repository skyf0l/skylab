apiVersion: v1
kind: ConfigMap
metadata:
  name: trivy-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Security"
data:
  trivy.json: |
    {{- .Files.Get "files/16337-trivy-operator-vulnerabilities.json" | nindent 4 }}
