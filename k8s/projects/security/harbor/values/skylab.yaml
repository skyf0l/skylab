# Per-cluster R2 coordinates for skylab. The access key/secret are NOT here —
# they come from Vault via ESO (kvv2/cluster/skylab/apps/harbor). Same R2
# account as thanos/loki, own bucket (terraform/cloudflare/r2.tf).
harbor:
  persistence:
    imageChartStorage:
      s3:
        bucket: skylab-harbor
        regionendpoint: https://2620dc6ee3d578b27347d8e5efd95f32.r2.cloudflarestorage.com
