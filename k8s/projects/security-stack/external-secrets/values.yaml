external-secrets:
  # Install the ESO CRDs (ExternalSecret, ClusterSecretStore, ...) with the chart.
  installCRDs: true

  # Prometheus metrics + ServiceMonitor for the External Secrets dashboard.
  metrics:
    service:
      enabled: true
  serviceMonitor:
    enabled: true
