apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tail-wildcard
spec:
  secretName: {{ .Values.tlsSecretName }}
  issuerRef:
    name: {{ .Values.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.baseDomain }}"
    - "*.{{ .Values.baseDomain }}"
