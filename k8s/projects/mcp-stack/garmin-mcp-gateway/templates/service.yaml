apiVersion: v1
kind: Service
metadata:
  name: garmin-mcp-gateway
  labels:
    app: garmin-mcp-gateway
spec:
  type: ClusterIP
  selector:
    app: garmin-mcp-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
