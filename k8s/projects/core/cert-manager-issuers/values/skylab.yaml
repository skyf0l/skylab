# ACME email. At deploy time the ApplicationSet overrides this with
# admin@<global.domain> via a Helm --set parameter; it's set here too so
# standalone `make template` (which doesn't inject that param) renders.
# Replace with a real mailbox to receive Let's Encrypt expiry notices.
email: admin@skyf0l.dev
