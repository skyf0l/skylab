apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Monitoring"
data:
  prometheus.json: |
    {{- .Files.Get "files/19268-prometheus.json" | nindent 4 }}
